import db_conn
from ase.io import write
import lattice_constant

print(lattice_constant.Li, type(lattice_constant.Li))
# data = db_conn.get_data(10)

# for d in data:
#     atom = str(d[0])
    # lat_con = lattice_constant.
    # print(lattice_constant)

# view(s1)